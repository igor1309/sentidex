# Repository Instructions

## GitHub Operations

Before interacting with GitHub, check whether the GitHub CLI is installed and authenticated with `gh --version` and `gh auth status --hostname github.com`.

When the GitHub CLI is available and authenticated, use it first for repository, workflow, issue, pull request, and other GitHub operations. Use browser interaction only when the CLI cannot perform the required operation or a UI-only action is needed.
