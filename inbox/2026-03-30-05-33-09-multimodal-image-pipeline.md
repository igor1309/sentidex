---
id: mncyqvgovd8oaha6chb
created_at: '2026-03-30T05:33:09.000Z'
processed_at: '2026-03-30T09:05:58.728Z'
source_info: Пух
has_media: false
language: mixed
summary: >-
  Создать механизм приема изображений, их промпты и JSON-вывод с фильтрацией по
  базе данных.
tags:
  - image-gen
  - ai
  - workflow
  - prompting
debug:
  message_ids:
    - 1147
  bundle_start_at: '2026-03-30T05:33:09.000Z'
  bundle_end_at: '2026-03-30T05:33:09.000Z'
  bundle_status: ambiguous
  forwarded_messages:
    - message_id: 1147
      timestamp: '2026-03-30T05:33:09.000Z'
      content: >-
        Итак, что тебе надо сделать.


        Тебе нужно создать простенький этап прогона картинок в их текстовое
        описание. 


        Нейронку проси, сделай мне механизм, который будет получать на вход
        картинку, и отправлять в другую мультимодальную (принимающую и текст и
        картинки) нейронку картинку+промпт, чтобы на выходе получить
        структурированный вывод в виде JSON, в котором будут заполнены
        характеристики картинки из предоставленных на выбор значений. Все
        значения считываются из нашей базы данных, по которым пользователи могут
        отфильтровать в поиске по определенным критериям выдачу, и получить
        только те картинки, которые будут соответствовать выбранным критериям
        фильтра.


        Итак, значит на вход идет картинка, и примерно такой промпт


        ```html

        <ROLE>

        Ты - анализатор изображений по предоставленному перечню параметров. Твоя
        цель заполнить JSON по нашему формату.

        </ROLE>


        <OBJECTIVE>

        Тут детально расписываешь задачу для нейронки, рассказываешь, какие
        параметры можно выбирать только на выбор одно, или выбрать несколько,
        если это теги, например, или что-то еще. В общем описываешь порядок
        действий ей что как заполняем в каком формате с какими ограничениями

        </OBJECTIVE>


        <JSON_SCHEMA>

        {
          "image": [
              "tags": {
              'тег1', 'тег2', 'тег3', 'тег4'
              },
              "category": {
              'категория_изображения'
              },
              "color": {
              'цветовая_схема'
              },
              "style": {
              'стиль_изображения'
              }
          ]
        }

        </JSON_SCHEMA>


        <IMAGE_CHARACTERISTICS>

        объясняешь какой параметр что означает, например параметр category - это
        выбор категории изображения из предоставленного списка категорий.  Можно
        прямо в промпт вставить переменную, например $categoryList и в нее
        подгружать из вашей БД все существующие категории на выбор, а нейронка
        только выбрала одно из списка. Например:


        Список всех доступных категорий для выбора: $categoryList 

        Список всех доступных тегов для выбора: $tagList 

        </IMAGE_CHARACTERISTICS>


        <OUTPUT_RULES>

        - Верни только JSON без markdown и без code fences.

        - Не добавляй текст вне JSON.

        - Строго соблюдай JSON_SCHEMA: не добавляй поля вне схемы и не пропускай
        обязательные блоки.

        </OUTPUT_RULES>

        ```

        нейронка тебе возвращает заполненный JSON, сохраняешь в базу данных его,
        с прикреплением ID обработанного изображения, и имя файла изображение
        которое только что обработали. А само изображение должно сохраняться в
        какую-то специальную папку откуда они в будущем будут подгружаться.


        И когда ты будешь поиском и фильтрами пользоваться, будешь искать по
        тегам, поиск из бд посмотрит что по этим параметрам есть вот такая вот
        картинка, вот ее айди, и подгружает ее.


        Очень топорно объяснил но ты это сообщение в нейронку засунь, она всё
        поймет и реализует.


        Советы: сразу ресайзить изображения при анализе до размеров помельче
        например 800х600 чтобы жрало меньше токенов при анализе. Но в хранилище
        сохранять оригинал чтоб четко на сайте отображалось. А ресайз версию
        вычищать после успешной обработки.


        Для обработки обязательно использовать систему очередей, чтобы можно
        было грузить сразу несколько картинок и они одна за одной обработаются в
        порядке очереди. Добавить ретри логику, если что то сломается или пойдет
        не так, делать попытки повтора.
      source_info: Пух
      source_url: ''
      forward_date: '2026-03-29T14:19:59.000Z'
      has_media: false
      media_type: none
      forward_protected: true
  source_metadata:
    - message_id: 1147
      message_type: forward
      source_info: Пух
      source_url: ''
      forward_protected: true
  ambiguity_reason: orphan_forward
---
==== FORWARDS ====

---- Forward 1 (message_id: 1147) ----
Source: Пух

Итак, что тебе надо сделать.

Тебе нужно создать простенький этап прогона картинок в их текстовое описание. 

Нейронку проси, сделай мне механизм, который будет получать на вход картинку, и отправлять в другую мультимодальную (принимающую и текст и картинки) нейронку картинку+промпт, чтобы на выходе получить структурированный вывод в виде JSON, в котором будут заполнены характеристики картинки из предоставленных на выбор значений. Все значения считываются из нашей базы данных, по которым пользователи могут отфильтровать в поиске по определенным критериям выдачу, и получить только те картинки, которые будут соответствовать выбранным критериям фильтра.

Итак, значит на вход идет картинка, и примерно такой промпт

```html
<ROLE>
Ты - анализатор изображений по предоставленному перечню параметров. Твоя цель заполнить JSON по нашему формату.
</ROLE>

<OBJECTIVE>
Тут детально расписываешь задачу для нейронки, рассказываешь, какие параметры можно выбирать только на выбор одно, или выбрать несколько, если это теги, например, или что-то еще. В общем описываешь порядок действий ей что как заполняем в каком формате с какими ограничениями
</OBJECTIVE>

<JSON_SCHEMA>
{
  "image": [
      "tags": {
      'тег1', 'тег2', 'тег3', 'тег4'
      },
      "category": {
      'категория_изображения'
      },
      "color": {
      'цветовая_схема'
      },
      "style": {
      'стиль_изображения'
      }
  ]
}
</JSON_SCHEMA>

<IMAGE_CHARACTERISTICS>
объясняешь какой параметр что означает, например параметр category - это выбор категории изображения из предоставленного списка категорий.  Можно прямо в промпт вставить переменную, например $categoryList и в нее подгружать из вашей БД все существующие категории на выбор, а нейронка только выбрала одно из списка. Например:

Список всех доступных категорий для выбора: $categoryList 
Список всех доступных тегов для выбора: $tagList 
</IMAGE_CHARACTERISTICS>

<OUTPUT_RULES>
- Верни только JSON без markdown и без code fences.
- Не добавляй текст вне JSON.
- Строго соблюдай JSON_SCHEMA: не добавляй поля вне схемы и не пропускай обязательные блоки.
</OUTPUT_RULES>
```
нейронка тебе возвращает заполненный JSON, сохраняешь в базу данных его, с прикреплением ID обработанного изображения, и имя файла изображение которое только что обработали. А само изображение должно сохраняться в какую-то специальную папку откуда они в будущем будут подгружаться.

И когда ты будешь поиском и фильтрами пользоваться, будешь искать по тегам, поиск из бд посмотрит что по этим параметрам есть вот такая вот картинка, вот ее айди, и подгружает ее.

Очень топорно объяснил но ты это сообщение в нейронку засунь, она всё поймет и реализует.

Советы: сразу ресайзить изображения при анализе до размеров помельче например 800х600 чтобы жрало меньше токенов при анализе. Но в хранилище сохранять оригинал чтоб четко на сайте отображалось. А ресайз версию вычищать после успешной обработки.

Для обработки обязательно использовать систему очередей, чтобы можно было грузить сразу несколько картинок и они одна за одной обработаются в порядке очереди. Добавить ретри логику, если что то сломается или пойдет не так, делать попытки повтора.
